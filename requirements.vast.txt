# Runtime and evaluation dependencies for the Vast.ai RTX 3090 image.
# torch is provided by the pinned Vast PyTorch base image.
# llama-cpp-python is intentionally excluded; Vast uses HF models on CUDA.

# Required when installing this file directly outside the Docker build.
--extra-index-url https://download.pytorch.org/whl/cu126

# PDF/OCR and chunking support
PyMuPDF
unstructured[pdf]
pytesseract
langchain-text-splitters
nltk

# HuggingFace embedding and generation
# Keep the CUDA stack supplied by the Vast base image. Without these pins,
# pip may resolve a newer PyTorch release and download a second CUDA stack.
torch==2.6.0+cu126
torchvision==0.21.0+cu126
sentence-transformers
transformers
accelerate
huggingface_hub

# Vector database and numerical dependencies
chromadb
numpy
scikit-learn
scipy
tqdm

# Data and evaluation
psutil
pandas
openpyxl
sacrebleu
rouge-score

# Runtime UI and lightweight validation
streamlit
altair
gitpython
watchdog
pytest
