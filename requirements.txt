# ===================================
# RAG Skripsi - Requirements
# ===================================
# Python version: 3.11.9 (install manually, not via pip)

# PDF Processing & Text Extraction
# ---------------------------------
PyMuPDF  # fitz - ekstraksi teks dari PDF
unstructured[pdf]  # element-based chunking
pdfplumber  # alternative PDF parsing
pytesseract  # OCR support untuk unstructured

# Text Processing & Chunking
# ---------------------------
langchain-text-splitters # RecursiveCharacterTextSplitter
# maxmin-chunker: custom implementation (src/chunking/maxmin_chunker.py), not a PyPI package
nltk # sentence tokenization untuk maxmin chunker

# Embeddings & Vector Database
# -----------------------------
sentence-transformers # model embedding (Qwen3-Embedding)
torch # PyTorch untuk model embedding
transformers  # Hugging Face transformers
accelerate    # diperlukan untuk device_map='auto' saat load HF model
chromadb # vector database

# Scientific Computing
# --------------------
numpy  # array operations
scipy  # scientific computing

# Utilities
# ---------
tqdm  # progress bars
python-dotenv # environment variables management

# Data Processing & I/O
# ----------------------
pandas
openpyxl         # diperlukan untuk pd.read_excel() membaca file .xlsx

# Optional: Visualization & Analysis
# -----------------------------------
matplotlib
seaborn
# Development Tools
# -----------------
pytest # unit testing
black  # code formatter
flake8 # linter
mypy # type checker
ipython  # enhanced REPL
jupyter # notebook support

# Evaluation
# ----------
ragas               # BLEU + ROUGE-L via ragas.metrics.collections (BleuScore, RougeScore)
                    # ragas menggunakan sacrebleu (corpus_bleu) dan rouge-score secara internal
rank-eval           # Mean Reciprocal Rank (MRR)
rapidfuzz           # Diperlukan oleh NonLLMContextPrecisionWithReference
sacrebleu           # Dependency ragas BleuScore (sacrebleu.corpus_bleu)
rouge-score         # Dependency ragas RougeScore (rouge_score.rouge_scorer)

# llama-cpp-python: diperlukan jika menggunakan GGUF embedder/generator
# Install manual dengan CUDA (RTX 3090, sm86):
#   CMAKE_ARGS="-DGGML_CUDA=on" pip install llama-cpp-python --force-reinstall --no-cache-dir
llama-cpp-python

# flash-attn: opsional, meningkatkan throughput ~20-40% di RTX 3090
# Install manual (butuh CUDA toolkit + torch sudah terinstall):
#   pip install flash-attn --no-build-isolation
# flash-attn

# Opsional: hanya dibutuhkan jika menggunakan --eval_llm_type (LLM-based Precision/Recall)
# Untuk Qwen sebagai evaluator: serve via vLLM/SGLang lalu pakai --eval_llm_type local
# langchain-openai

# Streamlit
# --------
streamlit  # UI annotation app + RAG chat demo

# Logging & Monitoring
# --------------------
loguru # advanced logging (optional)
