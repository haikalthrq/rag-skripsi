# Backup Downloaders

These scripts are kept for the laptop setup only. They use the GGUF embedding
model and FP8 generator intended for the local RTX 4050 workflow.

Do not use them for Vast.ai RTX 3090 runs. Use the active entry point instead:

```bash
python scripts/download_vast_assets.py
```
