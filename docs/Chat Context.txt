# Chat Context - RAG Skripsi Project
# Generated: 2026-05-30 03:12 WIB (UTC+7)
# Purpose: Summary for AI agents continuing this conversation

## Objective
Complete batch evaluations (Strict & Lenient), fix metrics computation issues, and ensure stable Streamlit operation for the RAG pipeline evaluation.

## Completed Tasks

### 1. Model & Format Update (2026-05-29)
- **Action:** Reverted to HuggingFace safetensors model (`models/Qwen3-Embedding-4B/`).
- **Reason:** HF model provides better retrieval performance (BF16 vs GGUF Q8_0).
- **Update:** pipeline.py now supports dual mode via `embedder_mode` parameter ('gguf' / 'huggingface').
- **Note:** Laptop still uses GGUF; Vast AI now uses HuggingFace model.

### 2. Streamlit UI Enhancements (`src/streamlit/rag_chat.py`)
- **Action:** Added "Mode Relevance" radio button (All, Strict, Lenient) to Evaluation tab.
- **Action:** DEFAULT_TOP_K=8, slider max=10.
- **Action:** Added progress feedback for pre-compute embedding phase to prevent "stuck" appearance.

### 3. BLEU/ROUGE Fix (2026-05-28)
- **Issue:** ragas 0.4.3 threw `No module named 'langchain_community.chat_models.vertexai'` error.
- **Fix:** Switched to direct library usage in `src/evaluation/metrics.py`:
  - BLEU: `sacrebleu.corpus_bleu()` directly (normalized 0-1).
  - ROUGE-L: `rouge_score.RougeScorer` directly, `use_stemmer=False`.

### 4. Cache Embedding Optimization (2026-05-28)
- **Action:** Pre-compute query embeddings once per QA before top-k loop in `_run_eval_and_save`.
- **Impact:** ~15-20% faster per QA (saves 5 embed calls per QA for top-k range 5-10).
- **Safety:** 100% safe — same input, same embedder, same output vector. No change to results.

### 5. Batch Evaluation Status
- **Strict Mode:** ✅ Completed (10/10 files, top-1 to top-10, `results/final/generation/strict/`).
- **Lenient Mode:** ✅ Completed (10/10 files, top-1 to top-10, `results/final/generation/lenient/`).

### 6. Environment Setup (2026-05-29)
- **Action:** Created virtual environment `.venv/` di folder rag-skripsi.
- **Action:** Installed all dependencies from `requirements.txt`.
- **Action:** Installed GitHub CLI (`gh`) dan authenticated.
- **Action:** Pushed changes ke GitHub main branch.

### 7. Results Restructure (2026-05-30)
- **Action:** Restructured `results/` folder:
  - `results/final/generation/strict/` — definitive strict results (top1–top10)
  - `results/final/generation/lenient/` — definitive lenient results (top1–top10)
  - `results/archive/` — all old/legacy runs organized by GPU
- **Code updates:** rag_chat.py, rank_results.py, run_generation_eval.py paths updated.

### 8. Hardware (Vast AI)
- **GPU:** NVIDIA A100 SXM4 40GB VRAM.
- **VRAM Usage:** ~8-9 GB utilized (Generator on GPU, Embedder on GPU via HF).
- **System:** Linux, stable.
- **Virtual Environment:** `.venv/` di folder rag-skripsi

## Current State

### Dependencies
- ✅ All requirements installed in `.venv/` (virtual environment di folder rag-skripsi)
- ✅ GitHub CLI (gh) terinstall dan authenticated

### Embeddings & Models
- **Model (Embedder):** `models/Qwen3-Embedding-4B/` (HuggingFace safetensors, GPU via sentence-transformers).
- **Model (Generator):** `models/Qwen3-4B-Instruct-2507` (HF, BF16, GPU).

### Results
- **Strict:** `results/final/generation/strict/` — 10/10 files complete.
- **Lenient:** `results/final/generation/lenient/` — 10/10 files complete.

### Git Status
- Branch: main
- Latest commit: Restructure results folder: separate final vs archive
- Commit hash: 4ed1bde

## Known Issues (Resolved)
- ~~ragas dependency error~~ → Fixed by direct sacrebleu/rouge-score usage.
- ~~Embedder path mismatch~~ → Fixed by moving GGUF to root models/.
- ~~UI stuck on "Memulai evaluasi"~~ → Fixed by adding pre-compute progress captions.
- ~~Embedder mode hardcoded to GGUF~~ → Fixed by adding `embedder_mode` parameter ('gguf' / 'huggingface').

## File Locations
- **Results (Strict):** `results/final/generation/strict/`
- **Results (Lenient):** `results/final/generation/lenient/`
- **Model (Embedder):** `models/Qwen3-Embedding-4B/` (HuggingFace safetensors)
- **Model (Generator):** `models/Qwen3-4B-Instruct-2507/`
- **Metrics Code:** `src/evaluation/metrics.py`
- **Streamlit App:** `src/streamlit/rag_chat.py`
- **Pipeline:** `src/rag/pipeline.py`
- **Scripts:** `scripts/run_generation_eval.py`, `scripts/run_retrieval_eval.py`

## Next Steps
1. Re-run retrieval evaluation with HF embedder (expecting better scores).
2. Analyze evaluation results across all 3 methods.
3. Compare Strict vs Lenient relevance mode performance.
4. Penulisan laporan skripsi.
