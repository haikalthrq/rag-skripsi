# Chat Context - RAG Skripsi Project
# Updated: 2026-06-05 04:01 WIB
# Purpose: Fast handoff summary for the next AI session

## Objective
Project sudah masuk fase akhir: pipeline RAG, ground truth, evaluasi, dan demo chat
sudah berjalan. Fokus aktif sekarang adalah finalisasi visualisasi, analisis hasil,
dan penulisan skripsi.

## Current Active Assets

- QA gold aktif:
  - `data/ground_truth/qa_gold_standard_rag_bps_30qa_question_newest.xlsx`
- GT retrieval aktif:
  - `data/ground_truth/qa_pairs_binary.json`
- Annotation final:
  - `data/ground_truth/retrieval_labels_final.csv`
  - `data/ground_truth/retrieval_labels_final.xlsx`
- Demo chat aktif:
  - `src/streamlit/rag_chat.py`
- Notebook visualisasi aktif:
  - `notebooks/eval_visualization.ipynb`
- Hasil final generasi:
  - `results/final/generation/`
- Hasil visualisasi:
  - `results/final/figures/`

## Important Decisions Locked In

- Evaluasi retrieval sekarang hanya pakai skema binary `0/1`.
- Label lama `1` dan `2` digabung menjadi `1 = relevan`.
- Input chat di `rag_chat.py` tidak lagi bebas; sekarang selectbox QA gold.
- `DEFAULT_TOP_K = 8`.
- `max_tokens = 16384`.
- Metrik retrieval ditampilkan inline di chat:
  - Precision@k
  - Recall@k
  - MRR
- Metrik generasi ditampilkan inline:
  - BLEU
  - ROUGE-L Recall
- Riwayat chat persisten di:
  - `results/chat_history/chat_history.jsonl`

## Latest Completed Work

### 1. Refactor GT dan evaluasi ke binary
- `qa_pairs_binary.json` menjadi GT retrieval aktif.
- Strict mode tidak lagi dipakai pada kode aktif.
- Kolom anotasi aktif sekarang bernama `label`.

### 2. Update `rag_chat.py`
- Input query diubah dari free text menjadi selectbox QA gold.
- Retrieval metrics ditampilkan inline bersama metrik generasi.
- Riwayat chat dibuat persisten.

### 3. Cleanup repo
- Beberapa script usang dan file redundan sudah dihapus/diarsipkan.
- Hasil generasi final sudah dirapikan ke folder flat:
  - `results/final/generation/`

### 4. Visualisasi evaluasi
- Visualisasi sekarang dipusatkan di:
  - `notebooks/eval_visualization.ipynb`
- Output utama:
  - `chart1_recall_trend.png`
  - `chart2_mrr_trend.png`
  - `chart3_generation_metrics.png`
  - `chart4_hit_count.png`
  - `chart5_heatmap_mrr.png`
- Tabel sudah diubah agar center aligned dan hanya bold nilai maksimum.
- Legend, header tabel, dan penempatan label chart sudah beberapa kali dituning.
- Chart 1 Recall memakai aturan label terbaru:
  - Top-k 5-7 semua label di atas garis.
  - Top-k 8-10: MaxMin di atas, Element di bawah, Recursive di atas.
  - Top-k 8 punya offset ekstra agar `0.510` dan `0.517` tidak overlap.
- Chart 3 legend sudah dipindah ke luar area plot.
- Table 3 header sudah sejajar dan tidak lagi menampilkan literal `\n`.
- File output lama di `results/final/figures/` yang bernama `01_*` sampai `06_*`
  dan `summary_metrics_*.csv` sudah dihapus karena tidak dipakai lagi.

## Current Repo State

- Branch aktif: `main`
- Remote tracking: `origin/main`
- Perubahan yang masuk ke push terbaru:
  - perubahan `notebooks/eval_visualization.ipynb`
  - regenerasi figure/CSV aktif di `results/final/figures/`
  - penghapusan notebook visualisasi lama `notebooks/00_*.ipynb` s.d. `07_*.ipynb`
  - penghapusan figure/summary lama di `results/final/figures/`
  - cache visualisasi `notebooks/_cache/data_eval.pkl`

## Important File State Notes

- Notebook visualisasi lama yang terpisah per chart tidak lagi jadi basis kerja;
  sekarang semua visualisasi aktif ada di `notebooks/eval_visualization.ipynb`.
- `results/final/figures/` sekarang hanya menyimpan output visualisasi aktif:
  `chart1` sampai `chart5` dan `table1` sampai `table4`.
- Jika ada perbedaan antara konteks lama dan repo sekarang, prioritaskan file aktual
  di working tree.

## Valid Reference Metrics

Top-k = 5:
- Generation:
  - Element: BLEU `0.1178`, ROUGE-L `0.7005`
  - MaxMin: BLEU `0.1052`, ROUGE-L `0.6369`
  - Recursive: BLEU `0.1440`, ROUGE-L `0.6838`
- Retrieval:
  - Element: P@5 `0.129`, R@5 `0.392`, MRR `0.354`
  - MaxMin: P@5 `0.131`, R@5 `0.447`, MRR `0.349`
  - Recursive: P@5 `0.145`, R@5 `0.352`, MRR `0.429`

## Next Practical Focus

1. Lanjutkan analisis hasil untuk naskah skripsi.
2. Finalisasi justifikasi nilai `k` yang dipakai di laporan.
3. Rapikan environment/lockfile bila diperlukan.
